name: baykodasansor
description: "Baykod Asansör Takip Sistemi Flutter Uygulaması"
publish_to: 'none'

# ⚠️ iOS KURALI:
# App Store / TestFlight her upload'ta build number (+) artmak ZORUNLU
version: 1.0.0+2

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🍎 iOS UI ikonları
  cupertino_icons: ^1.0.8

  # 🌐 API (Retrofit muadili)
  http: ^1.2.1

  # 🔐 Güvenli token & role saklama
  flutter_secure_storage: ^9.2.2

  # 🔔 Firebase (FCM – Push Notification)
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # 🖼️ iOS + Android App Icon Generator (ZORUNLU)
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  # 🖼️ ASSETS
  assets:
    - assets/

# 🖼️ APP ICON CONFIG (App Store uyumlu)
flutter_launcher_icons:
  ios: true
  android: true
  image_path: assets/icon/app_icon.png
  remove_alpha_ios: true
