name: baykodasansor
description: "Baykod Asansör Takip Sistemi Flutter Uygulaması"
publish_to: 'none'

# ⚠️ iOS KURALI:
# App Store / TestFlight her upload'ta build number (+) artmak ZORUNLU
version: 1.0.0+3

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🍎 iOS UI ikonları
  cupertino_icons: ^1.0.8

  # 🌐 HTTP API (Retrofit muadili)
  http: ^1.2.1

  # 🔐 Güvenli token & role saklama
  flutter_secure_storage: ^9.2.2

  # 🔥 FIREBASE CORE (ZORUNLU)
  firebase_core: ^4.4.0

  # 🔔 FIREBASE CLOUD MESSAGING
  firebase_messaging: ^16.1.1

  intl: ^0.19.0

  flutter_local_notifications: ^17.0.0

  # 📦 (OPSİYONEL – ŞİMDİ KULLANMIYORUZ)
  # İleride kullanıcı davranışı, hata analizi için açılabilir
  # firebase_analytics: ^10.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # 🖼️ iOS + Android App Icon Generator
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  # 🖼️ ASSETS
  assets:
    - assets/

# 🖼️ APP ICON CONFIG (App Store uyumlu)
flutter_launcher_icons:
  ios: true
  android: true
  image_path: assets/icon/app_icon.png
  remove_alpha_ios: true
